/*function divide(x, y) {
  const solution = x / y
  return (x / y)
}
console.log(divide(100, 10));

for (var x = 0; x < 5; x++) {

  console.log(divide(10000, 5));
}

function multiply(x, y) {
  const answer = x * y;
  return answer
}
console.log(multiply(100, 10));


function multiplyA(a, b) {
  const answerA = a * b
  return answerA
}
console.log(multiplyA(10, 4));

function multiply(x, y) {
  const solu = x / y;
  return solu
}
console.log(multiply(10, 5));


/*const temp = prompt("what is the temperature ?");


function tempCheck(arg) {
    if (
        temp >= arg


    )

        console.log("You dont need a coat!");


    if (
        temp < arg


    )

        console.log("You  need a coat!");

}
*/

//const temp = prompt("what is the temperature ?");//

//function tempChk(arg) {//


/*function checkTemperature(temperature) {

    if (temperature < 0) {
        return "Stay inside";
    } else if (temperature < 30) {
        return "Wear a coat and a hat";
    } else if (temperature < 50) {
        return "Wear a coat";
    } else {
        return "Just pants and vest is fine";
    }
}
    


console.log(checkTemperature(30));*/

/*const temprature = prompt("Write Temprature in Celsius");
function temp(t) {
    if (temprature <= 50 && temprature > 30) {
        return "Wear a coat";
    } else if (temprature <= 30 && temprature > 0) {
        return "Wear a coat and a hat";
    } else if (temprature <= 0) {
        return "Stay Inside";
    } else {
        return "just pants and vest is fine";
    }
}

console.log(temp(temprature20));*/

/* var userNumber = prompt("Choose a number")

var userNumberTwo = prompt("Choose another to be multiplied by the first number")

function multiply(a, b) {

    const answer = userNumber * userNumberTwo;
    return answer

}

console.log(answer);*/

const leagueFinish = prompt("Where did Leeds finish the season ?");

function leagueFinishCheck(f) {
    if (leagueFinish <= 24 && leagueFinish > 22) {
        return "Leeds were relegated";
    } else if (leagueFinish <= 21 && leagueFinish >= 7) {
        return "Leeds stayed in the championship";
    } else if (leagueFinish <= 6 && leagueFinish >= 3) {
        return "Leeds lost in the playoffs";
    } else {
    }
    return "Leeds were promoted";



}
console.log(leagueFinishCheck(leagueFinish));



























































